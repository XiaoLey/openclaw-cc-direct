{
  "id": "openclaw-cc-direct",
  "name": "Claude Code Direct",
  "description": "Control Claude Code from chat platforms via OpenClaw — direct CLI execution, zero network overhead",
  "version": "0.2.4",
  "skills": ["skills/cc-direct"],
  "contracts": {
    "tools": ["cc_send", "cc_plan", "cc_execute", "cc_workspace", "cc_reset", "cc_status"]
  },
  "commandAliases": [
    { "name": "cc", "kind": "runtime-slash" },
    { "name": "cc_plan", "kind": "runtime-slash" },
    { "name": "cc_execute", "kind": "runtime-slash" },
    { "name": "cc_workspace", "kind": "runtime-slash" },
    { "name": "cc_reset", "kind": "runtime-slash" },
    { "name": "cc_status", "kind": "runtime-slash" }
  ],
  "activation": {
    "onStartup": true,
    "onCommands": ["cc", "cc_plan", "cc_execute", "cc_workspace", "cc_reset", "cc_status"]
  },
  "configSchema": {
    "type": "object",
    "properties": {
      "model": {
        "type": "string",
        "enum": ["sonnet", "opus", "haiku", "sonnet[1m]", "opusplan"],
        "description": "Default Claude model to use. Can be overridden per-invocation. If omitted, uses Claude CLI default."
      },
      "allowedTools": {
        "type": "array",
        "items": { "type": "string" },
        "description": "List of Claude Code tool names to allow (e.g. [\"Bash\", \"Read\", \"Write\"]). If omitted, uses Claude Code defaults."
      },
      "env": {
        "type": "object",
        "additionalProperties": { "type": "string" },
        "description": "Extra environment variables passed to the Claude Code process (e.g. {\"ANTHROPIC_BASE_URL\": \"https://...\", \"ANTHROPIC_AUTH_TOKEN\": \"sk-...\"})"
      }
    },
    "additionalProperties": false
  }
}
